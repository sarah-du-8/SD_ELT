data_source yt_elt:
  type: postgres
  host: localhost
  port: "5432"
  username: ${ELT_DATABASE_USERNAME}
  password: ${ELT_DATABASE_PASSWORD}
  database: elt_db
  schema: core
  quote_identifiers: true